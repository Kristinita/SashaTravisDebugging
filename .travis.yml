cache:
  directories:
  - $HOME/.npm

dist: noble

git:
  depth: 1

install:
- nvm install node
- npm install --global pa11y-ci

language: python

os: linux

script:
- pa11y-ci --config .pa11yci.js https://pa11y.org/
