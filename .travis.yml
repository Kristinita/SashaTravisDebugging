before_install:
- pyenv global 3

before_script:
- cat /etc/os-release | grep "VERSION"
- python --version
- pip --version
- pydoclint --version

cache:
  directories:
  # - $HOME/.npm
  - $HOME/.cache/pip

dist: noble

git:
  depth: 1

install:
- python -m pip install --upgrade pip
- pip install pydoclint

language: shell

os: linux

script: skip
