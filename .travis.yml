before_script:
- npx pa11y-ci --version
- npx pa11y --environment

# cache:
#   directories:
#   - $HOME/.npm

dist: noble

git:
  depth: 1

install:
- nvm install node
- npm install

os: linux

script:
- npx pa11y --config .pa11yci.js https://pa11y.org/ --debug
- npx pa11y-ci --config .pa11yci.js https://pa11y.org/
