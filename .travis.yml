before_script:
- node --version
- npx flow --version
- npx flow-typed --version

cache:
  directories:
  - $HOME/.npm

dist: noble

git:
  depth: 1

install:
- nvm install node
- npm install
- npx flow-typed install

language: minimal

os: linux

script:
- npx flow kira-example.js
